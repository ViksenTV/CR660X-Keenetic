# Прошивка устройства в KeeneticOS

## 🧭 Способ №1 — Новый (рекомендуемый)

1. Смените загрузчик на **KeenBOOT**  
2. Загрузите **Firmware**, **EEPROM**, **U-Config** из архива **по очереди через веб-интерфейс** согласно инструкции  
3. Перезагрузите устройство  

**После прошивки:**

```
URL: http://192.168.1.1
Логин: admin
Пароль: 12345678

SSID: Keenetic
Пароль Wi-Fi: 12345678
```

---

## ⚙️ Способ №2 — Старый

1. В папке с **Keenetic** перетащите все `.bin`-файлы на `HFS.exe`.  
2. Для минимизации проблем с установкой рекомендуется выставить на сетевой карте IP-адрес:

```
192.168.1.2
```

3. Запустите **PuTTY** и подключитесь по **Telnet**:

```
Адрес: 192.168.1.1
Порт: 23
```

4. Вставляйте команды **по одной** (ПКМ → вставить), дожидаясь выполнения каждой.

---

### ⚠️ ПРЕДУПРЕЖДЕНИЕ

- Перед началом **отключите брандмауэр и антивирус** вашей ОС.  
- Сообщения  
  ```
  skipped bad blocks
  Flash erasure failed with -5
  ```  
  являются **нормой** — игнорируйте их.

---

### ❗ ВНИМАНИЕ

- Каждая строка — **отдельная команда**.  
  Если команда не выполнилась — **повторите её**.  
- Сравните IP в командах `wget` с IP, указанным в HFS.  
  В загрузчике **Breed** он всегда начинается с `192.168.1.xxx`.

---

### 🔧 Команды для прошивки

```bash
flash erase 0x80000 0x7f00000

wget http://192.168.1.2/u-config.bin
flash write 0x80000 0x80001000 0x80000
flash write 0x4040000 0x80001000 0x80000

wget http://192.168.1.2/EEPROM_XXX.bin
flash write 0x100000 0x80001000 0x80000
flash write 0x40c0000 0x80001000 0x80000

wget http://192.168.1.2/Firmware_N6_4.2.6.bin
flash write 0x180000 0x80001000 0x1AB3F00
flash write 0x4140000 0x80001000 0x1AB3F00

wget http://192.168.1.2/u-state.bin
flash write 0x3fc0000 0x80001000 0x80000

env set autoboot.command "boot flash 0x4140000"
env save
reset
```

---

После перезагрузки устройство запустится в **KeeneticOS** 🎉  

```
URL: http://192.168.1.1
Логин: admin
Пароль: 12345678

SSID: Keenetic
Пароль Wi-Fi: 12345678
```

---

